---
import "../styles/styles.css";
---

<div class="wave-wrapper">
  <section>
    <div class="teknologi">
      <div class="order">
        <img class="tbillede" src="/imgs/teknik.webp" alt="" />
      </div>

      <div class="ttekst">
        <h2>Teknologien bag LUMI</h2>
        <p>Med sin turbine-hydrauliske strømteknologi genererer LUMI Pure energi til UV-C lyset, hver gang vandet strømmer. Ingen batterier er nødvendige - en praktisk og bæredygtig løsning til rent drikkevand. Denne innovative teknologi sikrer altid sundt, velsmagende og filtreret vand, og den brugervenlige konstruktion gør det nemt at integrere LUMI Pure i din hverdag. Rent vand har aldrig været smartere.</p>
      </div>
    </div>
  </section>
</div>

<style>
  .wave-wrapper {
    position: relative;
    width: 100%;
    overflow: hidden; /* Skjuler det udenfor containeren */
    padding: 10em 0; /* Lodret luft */
  }

  .wave-wrapper::before {
    content: ""; /* Tilføjer bølgegrafik */
    position: absolute;
    top: 0;
    left: 0;
    width: 200%;
    height: 100%;
    background-image: url("/imgs/wave.svg");
    background-size: auto 100%;
    background-position: center;
    background-repeat: repeat-x;
    animation: waveScroll 20s linear infinite; /* Giver bevægelse */
    z-index: 0; /* Bølgens lag er bagved */
  }

  @keyframes waveScroll {
    from {
      background-position-x: 0;
    }
    to {
      background-position-x: -100%; /* Bølge glider mod venstre */
    }
  }

  .wave-wrapper section {
    position: relative;
    z-index: 1; /* Indhold ligger over bølgen */
    display: grid;
    padding: 0 1em;
    max-width: 1200px;
    margin: 0 auto;
  }

  .tbillede {
    max-width: 100%;
    height: auto;
    border-radius: 1em; /* Runde hjørner */
  }

  /* Mobil */
  @media (max-width: 768px) {
    .teknologi {
      display: flex; /* Lodret layout */
      flex-direction: column;
      align-items: center;
      padding: 2em 1em;
    }

    .ttekst {
      padding-top: 1em;
      margin: 0;
      width: 100%;
      max-width: 600px;
      text-align: left;
    }
  }

  /* Desktop */
  @media (min-width: 769px) {
    .teknologi {
      display: grid;
      grid-template-columns: 1fr 1fr; /* 2 kolonner */
      gap: 150px;
    }

    .order {
      display: block;
      order: +1; /* Flytter billede til højre */
      padding-right: 20px;
    }

    .ttekst {
      margin-top: 35px;
      margin-left: 70px;
    }
  }
</style>
